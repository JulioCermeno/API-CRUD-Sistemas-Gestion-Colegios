@model IEnumerable<Api_Web_Crud.Models.Profesores>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Edad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Materia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AñosExperiencia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Materia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AñosExperiencia)
            </td>
            <td>
                    <a asp-action="Editar" class="btn btn-warning" asp-route-id="@item.IdProfesor">Edit</a> |
                    <a asp-action="Consulta" class="btn btn-primary" asp-route-id="@item.IdProfesor">Details</a> |
                    <a asp-action="Borrar" class="btn btn-danger" asp-route-id="@item.IdProfesor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
